[build]
  base    = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — React Router handles all routes
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
